# Base URL of the registry-backend (tinox) instance this frontend talks to.
# Override at runtime with -Dregistry.backend.url=... or the
# REGISTRY_BACKEND_URL env var (Quarkus/MicroProfile Config convention).
quarkus.http.port=8082
registry.backend.url=http://localhost:8080
